<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.juego.proyecto_1s2122">

    <!-- Solicitar permisos de Bluetooth heredados en dispositivos más antiguos. -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <!-- Necesario para buscar dispositivos superiores a api 23. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/miEstilo">
        <activity
            android:name=".vistas.EstadisticasActivity"
            android:screenOrientation="landscape" />
        <activity
            android:name=".vistas.CrearUsuarioActivity"
            android:screenOrientation="landscape" />
        <activity
            android:name=".vistas.JuegoActivity"
            android:screenOrientation="landscape" />
        <activity
            android:name=".vistas.SalaEsperaActivity"
            android:screenOrientation="landscape" />
        <activity
            android:name=".vistas.BuscarPartidaActivity"
            android:screenOrientation="landscape" />
        <activity
            android:name=".vistas.CrearPartidaActivity"
            android:screenOrientation="landscape" />
        <activity
            android:name=".vistas.MenuActivity"
            android:screenOrientation="landscape" />
        <activity
            android:name=".vistas.MainActivity"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>